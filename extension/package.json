{
  "name": "stepshots-extension",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "bun run build.ts",
    "dev": "bun run build.ts --watch"
  },
  "devDependencies": {
    "@types/chrome": "^0.1.37"
  },
  "dependencies": {
    "@preact/signals": "^2.9.0",
    "fflate": "^0.8.2",
    "preact": "^10.29.1"
  }
}
