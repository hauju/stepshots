{
  "name": "stepshots-extension",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "bun run build.ts",
    "dev": "bun run build.ts --watch"
  },
  "devDependencies": {
    "@types/chrome": "^0.1.37"
  },
  "dependencies": {
    "fflate": "^0.8.2"
  }
}
